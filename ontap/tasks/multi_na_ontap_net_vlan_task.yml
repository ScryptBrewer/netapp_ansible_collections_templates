---
- name: Running command na_ontap_zapit
  na_ontap_zapit:
    hostname: "{{ netapp_hostname }}"
    username: "{{ netapp_username | default(omit }}"
    password: "{{ netapp_password | default(omit) }}"
    cert_filepath: "{{ netapp_cert_filepath | default(omit) }}"
    key_filepath: "{{ netapp_key_filepath | default(omit) }}"
    https: "{{ netapp_https | default(true) }}"
    port: "{{ netapp_port | default(omit) }}"
    validate_certs: "{{ validate_certs | default(false) }}"
    http_port: "{{ opt_lun_map_http_port_01 | default(omit) }}"
    ontapi: "{{ opt_lun_map_ontapi_01 | default(omit) }}"
    use_rest: "{{ opt_lun_map_use_rest_01 | default(omit) }}"
    node: 'item.{{ req_net_vlan_node_01 }}'
    parent_interface: 'item.{{ req_net_vlan_parent_interface_01 }}'
    vlanid: 'item.{{ req_net_vlan_vlanid_01 }}'
    feature_flags: 'item.{{ opt_net_vlan_feature_flags_01 | default(omit) }}'
    state: 'item.{{ opt_net_vlan_state_01 | default(omit) }}'
  register: results
