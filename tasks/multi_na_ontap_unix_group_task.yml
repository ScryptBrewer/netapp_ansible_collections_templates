---
- name: Running command na_ontap_unix_group
  na_ontap_unix_group:
    hostname: "{{ netapp_hostname }}"
    username: "{{ netapp_username }}"
    password: "{{ netapp_password }}"
    https: "{{ netapp_https | default(omit) }}"
    port: "{{ netapp_port | default(omit) }}"
    #validate_certs: "{{ validate_certs }}"
    name: '{{ item.req_unix_group_name_01 }}'
    vserver: '{{ item.req_unix_group_vserver_01 }}'
    feature_flags: '{{ item.opt_unix_group_feature_flags_01 | default(omit) }}'
    id: '{{ item.opt_unix_group_id_01 | default(omit) }}'
    skip_name_validation: '{{ item.opt_unix_group_skip_name_validation_01 | default(omit) }}'
    state: '{{ item.opt_unix_group_state_01 | default(omit) }}'
    users: '{{ item.opt_unix_group_users_01 | default(omit) }}'
  register: results
