---
- name: Running command na_ontap_nvme
  na_ontap_nvme:
    hostname: "{{ netapp_hostname }}"
    username: "{{ netapp_username }}"
    password: "{{ netapp_password }}"
    https: "{{ netapp_https | default(omit) }}"
    port: "{{ netapp_port | default(omit) }}"
    #validate_certs: "{{ validate_certs }}"
    vserver: '{{ item.req_nvme_vserver_01 }}'
    feature_flags: '{{ item.opt_nvme_feature_flags_01 | default(omit) }}'
    name: '{{ item.opt_nvme_name_01 | default(omit) }}'
    state: '{{ item.opt_nvme_state_01 | default(omit) }}'
    status_admin: '{{ item.opt_nvme_status_admin_01 | default(omit) }}'
  register: results
