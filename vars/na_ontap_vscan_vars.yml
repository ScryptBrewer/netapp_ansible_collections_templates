
# Section: na_ontap_vscan:
    hostname: "{{ netapp_hostname }}"
    username: "{{ netapp_username }}"
    password: "{{ netapp_password }}"
    https: "{{ netapp_https | default(omit) }}"
    port: "{{ netapp_port | default(omit) }}"
    #validate_certs: "{{ validate_certs }}"
    vserver: '{{ req_vscan_vserver_01 }}'
    enable: '{{ opt_vscan_enable_01 | default(omit) }}'
    feature_flags: '{{ opt_vscan_feature_flags_01 | default(omit) }}'
    name: '{{ opt_vscan_name_01 | default(omit) }}'
  
